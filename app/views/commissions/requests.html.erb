<!DOCTYPE html>
<html>
  <head>
    <title>
      CommissionMe
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
   
    <script type="text/javascript">
      function getAuthToken(){ 
        return "<%=form_authenticity_token%>"
      }
    </script>
    <div id="commissions" data-url="<%= @user.received_commissions %>" />
    <script src="/assets/reqlist.js"></script>  
  </head>
  <body>
    <div class="container">
      <div class="page-header">
        <h1>Commission Requests: <%=@user.name%>!</h1>
      </div>
      
      <!--div id="req-list"></div-->

      <div class="list">
	<ul>
	  <% @user.received_commissions.each do |commission| %>
	  <li><%=link_to "Commission: " + commission.commission_current["name"], 
		review_path(:commission_id => commission.id), 
            :class => "link"%> Status: <%=commission.state%></li>
	<% end %>
	</ul>
      </div>
      
    </div>
  </body>
</html>

