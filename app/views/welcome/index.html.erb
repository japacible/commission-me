<!DOCTYPE html>
<html>
    <head>
    <title>CommissionMe</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <%= stylesheet_link_tag :"bootstrap.min", :"bootstrap-responsive.min" %>
    <%= javascript_include_tag :"jquery", :"bootstrap.min" %>


    </head>
    <body>
		<div class="container">
		<% num_cols = 3 %>
		<% index = 0%>
	 	<%@featured_users.each_with_index do |user|%>
			<%if !user.get_featured_art %>
				<%next%>
			<%end%>
			<%if index % num_cols == 0 %>
				<div class="row"> 
			<%end%>
			  <div>
				<%=link_to(user, :class => "span3") do%>
			  		<img src="<%=user.get_featured_art.location%>">
				<%end%>
			  </div>
		
	
		<%if index%num_cols == num_cols-1 %>
			</div>
			<br /><br />
		<%end%>
		<%index+=1%>
	<% end %>
	</div>
  
  <!-- Render the partial template for the common footer -->
  <%= render "layouts/footer" %>
<!-- Commenting this out because 'javascript_include_tag' above seems to actually serve these files....
		<script src="assets/js/jquery.js"></script>
		<script src="assets/js/bootstrap.min.js"></script>
-->
    </body>
</html>
