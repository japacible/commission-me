<%

require "pg"
conn = PG::Connection.open(:dbname => 'd6nvftc6a9m3cj') 
res = conn.exec("select * from users;")

res.each{ |row|
  puts row["username"] + " // " + row["f_name"] + " // " + row["l_name"]
}
%>


<%= Time.now %>
